# Changelog

Published versions of this repository. The protocol id on the wire is `mixtrapi/1` until an incompatible change requires `mixtrapi/2`.

## Unreleased

First public Mixtrapi: spec, JSON Schema, JavaScript helpers, and conformance fixtures.
